apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nzbget
spec:
  rules:
  - http:
      paths:
      - path: /nzbget
        backend:
          serviceName: nzbget
          servicePort: 6789
