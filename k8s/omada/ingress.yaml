apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omada
spec:
  rules:
  - http:
      paths:
      - path: /omada
        backend:
          serviceName: omada
          servicePort: 8088
