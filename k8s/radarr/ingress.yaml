apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr 
spec:
  rules:
  - http:
      paths:
      - path: /radarr
        pathType: ImplementationSpecific
        backend:
          service:
            name: radarr
            port:
              number: 7878
